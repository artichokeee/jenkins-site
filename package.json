{
  "name": "testing-website",
  "version": "1.0.0",
  "description": "A simple website to learn testing",
  "main": "index.js",
  "scripts": {
    "test": "mocha tests/test.js",
    "build": "echo 'Building the website...' && mkdir -p dist && cp -r * dist/ && rm -rf dist/node_modules dist/tests dist/package.json dist/package-lock.json"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "assert": "^2.0.0",
    "jsdom": "^16.5.0",
    "mocha": "^8.3.2"
  }
}
