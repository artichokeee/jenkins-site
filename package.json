{
  "name": "testing-website",
  "version": "1.0.0",
  "description": "A simple website to learn testing",
  "main": "index.js",
  "scripts": {
    "test": "mocha --exit tests/test.js",
    "build": "echo 'Building the website...' && mkdir -p dist && rsync -av --progress --exclude dist --exclude node_modules --exclude tests --exclude package.json --exclude package-lock.json ./ dist/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "assert": "^2.0.0",
    "jsdom": "^16.5.0",
    "mocha": "^8.3.2"
  }
}
